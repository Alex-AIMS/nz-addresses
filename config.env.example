# NZ Addresses - Configuration File
# =====================================
# Copy this file to config.env and fill in your actual credentials
# DO NOT commit config.env to git (it's already in .gitignore)

# Database Credentials
# --------------------
# These are used by Docker container and connection strings
POSTGRES_USER=nzuser
POSTGRES_PASSWORD=nzpass
POSTGRES_DB=nz_addresses_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# API Keys
# --------
# Get your free LINZ API key from: https://data.linz.govt.nz/
# Register an account, then go to "My API Keys" to generate
LINZ_API_KEY=YOUR_LINZ_API_KEY

# Get your free Stats NZ API key from: https://datafinder.stats.govt.nz/
# Click "Sign In" → "Register" → "My Account" → "API Keys"
STATSNZ_API_KEY=YOUR_STATSNZ_KEY

# Connection String (automatically constructed from above)
# This is used by the .NET Web API
CONNECTION_STRING=Host=${POSTGRES_HOST};Port=${POSTGRES_PORT};Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}

# API Configuration
# ----------------
ASPNETCORE_URLS=http://0.0.0.0:8080
ASPNETCORE_ENVIRONMENT=Production
